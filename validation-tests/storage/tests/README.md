# Storage Test Fixtures

Test-local fixtures and standalone workload tests belong here. Framework contract and integration coverage currently lives in `tests/test_validation_scripts.py` and `tests/test_config.py` until U3/U5 fixture isolation is expanded.
